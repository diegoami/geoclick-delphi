unit uFileTreeNodes;

interface
{
type
  TFileTreeNodes = class(TTreeNodes)
     procedure Load(Dir : String; Node : TDrawNode);
  end;
 }
implementation

//procedure TFileTreeNodes.Load(Dir : String; Node : TDrawNode);

end.
